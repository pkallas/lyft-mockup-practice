{
  "name": "relaxitaxi-schema-design",
  "version": "1.0.0",
  "description": "Practice schema design",
  "main": "index.js",
  "scripts": {
    "db-init": "psql < schema.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pkallas",
  "license": "ISC",
  "dependencies": {
    "pg": "^7.2.0"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "mocha": "^3.5.0"
  }
}
